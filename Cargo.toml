[package]
name = "mdweb"
version = "0.1.0"
edition = "2021"
description = "Render a directory of Markdown into a live multi-language blog, in pure Rust, with no external dependencies."

[dependencies]
